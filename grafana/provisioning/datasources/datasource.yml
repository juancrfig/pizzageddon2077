apiVersion: 1
datasources:
  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: user
    secureJsonData:
      password: chaos2077
    jsonData:
      database: pizzageddon
      sslmode: disable  # For local
    isDefault: true
